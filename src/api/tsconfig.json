{
	"extends": "../../tsconfig-base.json",
	"compilerOptions": {
		"outDir": "../../out/api",
		"rootDir": ".",
		"paths": {
			"match": ["../match"],
			"schema": ["../schema"]
		}
	},
	"references": [
		{"path": "../match"},
		{"path": "../schema"}
	]
}
